{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "tsc: build - tsconfig.json",
      "type": "shell",
      "command": "source ~/.zshrc && ${workspaceFolder}/node_modules/.bin/tsc -p ${workspaceFolder}/tsconfig.json",
      "problemMatcher": ["$tsc"]
    }
  ]
}
